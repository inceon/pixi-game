var Engine = {
    minWidth: 640,
    minHeight: 320,
    maxWidth: window.innerWidth - 50,
    maxHeight: window.innerHeight - 50,
    config: {
        antialias: false,
        transparent: false,
        resolution: 1
    }
};
